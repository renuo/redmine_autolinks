<%= title t("autolinks_title_new") %>

<%= labelled_form_for @autolink, url: project_autolinks_path(@project) do |f| %>
  <%= render partial: "autolinks/form", locals: { f: f } %>
  <%= submit_tag t("button_create") %>
<% end %>
