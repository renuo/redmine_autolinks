<%= title t("autolinks_title_index") %>

<%= link_to t("autolinks_title_new"), new_project_autolink_path %>

<% @autolinks.each do |autolink| %>
  <p><%= link_to "#{autolink.prefix}-123", project_autolink_path(@project, autolink) %> →
     <%= autolink.target_url_with("123") %></p>
<% end %>
